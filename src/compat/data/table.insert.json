{
  "support": { "lua": { "version_added": "5.1" } },
  "changed_in": {
    "5.2": "Rejects a `pos` outside `1` to `#list + 1`; 5.1 checks no bounds — the value is stored at the position given, and a position below `1` also shifts the elements up and empties the front of the list, leaving no sequence behind.",
    "5.3": "Reads and writes elements through the table's metamethods; 5.1 and 5.2 access them raw."
  }
}

{
  "support": { "lua": { "version_added": "5.1" } },
  "changed_in": {
    "5.3": "Reads and writes elements through the table's metamethods, and checks that `comp` is a function only once the list holds more than one element; 5.1 and 5.2 access elements raw and check `comp` whatever the list holds."
  }
}

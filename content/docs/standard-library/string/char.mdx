---
title: string.char()
description: Build a string out of the numeric codes of its bytes.
lua-compat: string.char
entry-type: function
source: https://www.lua.org/manual/5.5/manual.html#pdf-string.char
---

Builds a string out of numbers, one byte per argument, taking as many of them as you care
to hand it. It is the inverse of
[`string.byte()`](/docs/standard-library/string/byte).

<RunnableExample code={`print(string.char(76, 117, 97))\n-- Expected output: Lua`} />

## Syntax

```lua
string.char(···)
```

<Parameters>
  <Param name="···">Zero or more byte values, each from 0 to 255. Each becomes one byte of the result, in the order given.</Param>
</Parameters>

<Returns>
  <Return type="string">A string exactly as long as the argument list, whose bytes are the values handed in. With no arguments, the empty string.</Return>
</Returns>

<Errors>

- Raises when an argument falls outside what a single byte holds — below zero, or above 255.
- Raises when an argument is neither a number nor a string that reads as one.

</Errors>

## Description

The result is always exactly one byte per argument: its length equals the number of values
given, and no value ever expands into more than one byte. `0` is an argument like any
other, producing a string with a zero byte in it, which Lua carries around like any other
byte rather than treating as the end of the string.

Which byte a given number stands for is settled by the platform rather than by the
language. What `string.char()` does promise is that it undoes
[`string.byte()`](/docs/standard-library/string/byte): a string taken apart into numbers
and handed straight back comes out unchanged, whatever those numbers happened to mean on
that machine.

Reaching for it to put one known byte into a literal is not what it is for. Lua's string
syntax already writes bytes directly — `"\t"` is a tab, and `"\27"` is any byte named by
its decimal value — so `string.char(9)` produces nothing `"\t"` does not, only longer.
`string.char()` earns its place when the number is *not* written down: held in a variable,
read out of data, or computed.

## Examples

### Rebuilding a string from its bytes

<RunnableExample code={`local word = "Lua"\nlocal rebuilt = string.char(string.byte(word, 1, -1))\nprint(rebuilt)\nprint(rebuilt == word)\n-- Expected output:\n-- Lua\n-- true`} />

`string.byte()` returns one value per byte, and those values land in `string.char()` as its
whole argument list, so the two compose into a round trip with no loop between them.

### Turning escaped byte values back into text

<RunnableExample code={`local encoded_path = "caf%C3%A9%20au%20lait"\nlocal decoded_path = string.gsub(encoded_path, "%%(%x%x)", function(hex_digits)\n  return string.char(tonumber(hex_digits, 16))\nend)\nprint(decoded_path)\n-- Expected output: café au lait`} />

Each `%C3` in that text is one byte written as two hexadecimal digits. `tonumber()` reads
the digits and `string.char()` turns the number back into the byte — and the two that
spell `é` are exactly the pair
[`string.byte()`](/docs/standard-library/string/byte) would have produced from it. Every
number reaching `string.char()` here is worked out while the program runs, from text it
had not seen when it was written — which is the situation the function exists for.

## Gotchas

<Gotcha title="The arguments are bytes, not code points">
Each argument fills exactly one byte, so a value above 255 raises rather than producing the
character with that code point — `string.char(0x20AC)` does not give `€`. Building that
character by hand means passing the three bytes it is encoded as,
`string.char(226, 130, 172)`, and
[`utf8.char()`](/docs/standard-library/utf8/char) is what does it from the code point
directly.
</Gotcha>

## See also

- [`string.byte()`](/docs/standard-library/string/byte) — the inverse, a string back into numbers
- [`utf8.char()`](/docs/standard-library/utf8/char) — building from code points instead of bytes
- [`string.format()`](/docs/standard-library/string/format) — `%c` writes a byte from its code inside a template
